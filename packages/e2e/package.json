{
    "name": "@mivend/e2e",
    "version": "0.1.0",
    "license": "GPL-3.0-or-later",
    "private": true,
    "scripts": {
        "test": "playwright test",
        "test:smoke": "playwright test --grep @smoke --project=storefront-noauth --project=storefront",
        "test:ui": "playwright test --ui",
        "report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "^1.49.0",
        "@types/node": "^20.0.0"
    }
}
