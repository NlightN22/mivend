query Departments {
    departments {
        id
        erpId
        name
    }
}

query TeamDirectory {
    teamDirectory {
        id
        firstName
        lastName
        roleCodes
        departmentId
        branchId
        position
    }
}
